/** @type {import('next').NextConfig} */
const nextConfig = {
  // Required for Cloudflare Pages via @cloudflare/next-on-pages
  // This tells Next.js to output an edge-compatible bundle
};

export default nextConfig;
